# Changelog

## v1.3.2

- 修复 MacBook / Safari 在三网胶囊条出现后滚动明显掉帧的问题：移除每个采样胶囊的模糊 `box-shadow`，避免同屏数百个 blur shadow 参与合成。
- 胶囊继续保留圆润形状，RGB 绿 / 橙 / 红改为更清晰明亮的纯色，不再依赖辉光制造亮度。
- 三网区域增加 paint containment，采样行增加 layout / paint containment，限制重绘范围。
- 1H Ping 查询、刷新频率、WebSocket、COBE、详情 7D 与节点卡片 DOM 数量均不改。


## v1.3.1

- 性能修正：首页节点卡片彻底移除 7D 历史预加载、缓存和定时刷新，恢复只使用原有 1H / 60 点 / ping-only 轻量查询。
- 7D 改到节点详情 History 的时间范围中，资源和网络延迟都支持 1H / 6H / 24H / 7D；只有用户点开详情并选择 7D 时才请求 `hours=168`。
- 保留 v1.3.0 的卡片宽度对齐和胶囊采样条；RGB 绿 / 橙 / 红略微提亮，仍保持克制辉光。
- 不改 WebSocket、COBE、详情图表交互和其它刷新逻辑。


## v1.3.0

- 节点卡片网格去掉左右二次内边距，手机与 PC 卡片总宽度和上方总览区域对齐；三列布局继续自适应。
- 三网卡片增加 7D 均值与 7D 丢包摘要；7D 数据使用官方公开上限 `hours=168`、`series=ping`，单并发、15 分钟缓存刷新。
- 三网采样条改为更圆润的胶囊小格，并使用克制的 RGB 绿 / 橙 / 红高亮与轻微辉光；不改现有延迟 / 丢包阈值。
- 当前延迟与 1H 丢包继续使用原有 1 小时轻量查询；详情 Drawer、历史图、COBE、WebSocket 刷新逻辑不变。


## v1.2.0

- 仅调整首页节点卡片：三网网络区改为 CF Atlas 风格的双列三行布局，左侧联通 / 电信 / 移动延迟，右侧对应三行丢包。
- 延迟与丢包各自增加 14 格最近样本条，直接复用现有 1H / 60 点 Ping 响应，不新增 API 请求、轮询器或刷新频率。
- 流量资源项标题后增加“每月 N 日重置”，继续保留已用、余量和配额进度。
- 桌面列表恢复原有 Load 副信息；详情 Drawer、历史图、COBE 地球、WebSocket 与现有性能策略均不改。


## v1.1.0

- 节点卡片把三网延迟提升为首页主信息：联通 / 电信 / 移动最新延迟无需进入详情即可查看。
- 卡片延迟保持单行三等分布局，只增加极少高度；桌面列表模式同步把延迟摘要放到节点副信息行。
- 首页延迟采用 1 小时、60 点、仅 Ping 序列的轻量查询，最多 2 个节点并发并每 60 秒刷新，避免影响现有 WebSocket 实时指标与详情历史图流畅度。
- 未配置 Ping Task 的节点自动隐藏延迟行；详情 Drawer、历史图、COBE 地球与现有视觉风格保持不变。


## v1.0.0

- 完成 1.0 发布审计：确认 Monitor 主题安装包、内容哈希资源、SPA 路由、在线更新与 Release 流程兼容当前上游。
- 保留 v0.9.8～v0.9.10 的首屏并行加载、COBE 延迟加载、实时 DOM 增量更新、单视图节点渲染与后台暂停策略。
- 移除无实际运行价值的 `llms.txt`；保留几乎零负担的 meta description、`robots.txt` 与正确 ARIA 语义。
- 主题切换按钮补充随状态变化的可访问标签，不改变现有视觉。
- 无效 `/node/{id}` 深链或详情节点被移除时自动返回概览，避免残留陈旧 Drawer / URL。
- 标签页从后台回到前台时立即同步一次节点数据，避免等待下一帧 WebSocket 才刷新。
- Release 校验加强：只允许一份主 app / Atlas CSS，禁止残留 `llms.txt`，继续执行运行时契约测试。
- Geist + Geist Mono、COBE 地球、节点卡片、历史图表、流量语义与桌面 / 手机布局保持不变。

## v0.9.10

- 节点区域只渲染当前实际视图：手机只生成卡片 DOM，桌面仅生成当前选择的列表或卡片，不再同时维护两套隐藏节点树。
- 监听 760px 响应式断点，跨手机 / 桌面尺寸时仅重建节点区域；桌面视图偏好继续保留。
- 删除双 DOM 模式遗留 CSS，并为离屏节点启用 `content-visibility:auto`，减少首屏布局与绘制工作。
- 节点行 / 卡片改为合适的 `div[role=button]` 结构，右侧箭头改为非交互装饰，修复 Lighthouse ARIA role 审核。
- 增加 meta description、有效的 `robots.txt` 与包含 H1 / 链接的 `llms.txt`。
- 保持 Geist + Geist Mono 字体、COBE 画质、API / WebSocket 与现有视觉不变。

## v0.9.9

- 修复直接打开 / 刷新 `/node/{id}` 时节点数据后到可能不补建详情 Drawer 的竞态。
- WebSocket 增加帧结构校验与连续坏帧保护；协议异常时自动退回 5 秒 HTTP 轮询，手动重试可恢复 WS。
- 后台标签页不再解析 / 渲染每 2 秒实时帧，轮询也暂停；回到前台后自动恢复。
- KPI 改为文本 / 曲线增量更新；Regions 与 COBE 仅在地区、数量或在线状态发生变化时刷新。
- HTTP / WS 短暂异常保留最后一份正常节点数据，不再 teardown 页面和重建地球。
- API 错误正文限制为 200 字符，并补充 401 状态页访问权限刷新处理。
- 国家代码统一校验为 ISO alpha-2 形状，异常值安全清空。
- 合并 v0.9.3～v0.9.7 节点卡片历史覆盖 CSS，移除重复 / 失效规则，保持现有视觉不变。
- Release 新增运行时契约测试：流量模式、坏 metrics、国家码、WS 帧、节点结构签名及关键性能约束。

## v0.9.8

- 首屏性能优化：`/api/me`、`/api/nodes` 与 WebSocket 并行启动，减少串行等待。
- COBE 地球改为首屏内容绘制后动态加载，不再阻塞主程序执行；页面隐藏时不主动初始化。
- 实时推送在节点结构未变化时只更新数字、进度条、在线状态，不再每 2 秒整块重建节点列表 / 卡片。
- 仅新增/删除节点或名称、国家、系统、架构、额度等结构字段变化时重建节点区。
- 明暗主题切换不再重渲染整页与重建 WebGL 地球。
- 所有视觉布局和现有交互保持不变。

## v0.9.7

- 卡片模式系统图标行取消额外左缩进。
- PC / 手机端系统图标左边缘与 CPU / DISK 资源列完全对齐。
- 列表模式与其他布局保持不变。

## v0.9.6

- PC 列表模式的系统图标去掉方框与 Tag，统一为裸图标。
- 卡片模式的系统图标左移，与上方国旗列对齐。
- PC / 手机端保持相同的系统图标视觉语言，其他布局不变。

## v0.9.5

- 节点卡片系统图标改回旧版 Atlas 风格：去掉方框 / Tag，只保留图标本体。
- PC 与手机端统一放大系统图标，提高辨识度。
- 其他节点卡片布局、流量进度与详情逻辑保持不变。

## v0.9.4

- 修复手机端流量进度条被旧版 `.traffic` 移动端规则误隐藏的问题。
- 新卡片流量进度条改用独立类名，桌面与手机统一显示。
- 放大 PC 与手机节点卡片的系统图标，恢复接近旧版 Atlas 的视觉比例。
- 手机端资源进度条略增粗，提高低占用比例下的可见性。

## v0.9.3

- 手机端节点改为紧凑卡片：CPU / RAM / DISK / 流量采用 2×2 半宽资源区。
- 流量上移为主资源项，显示本周期已用、真实剩余与使用进度。
- 卡片底部保留实时上下行，并增加累计上下行。
- 桌面端新增“列表 / 卡片”切换并记忆选择；原有列表模式保持可用。
- 地球、顶部 KPI、详情页与历史图表逻辑不变。

## v0.9.2

- 按 Monitor 官方主题开发规范重新审查安装包、manifest、API、路由与发布流程。
- 所有 `/assets/*` 文件统一改为内容哈希文件名，避免 hub 一年 immutable 缓存导致图标或字体更新不生效。
- 删除未使用的 Geist Pixel 字体、旧 Atlas SVG 品牌图标、废弃 livePing 状态与 COBE 空 visibility 监听。
- 收紧 `/node/{id}` 路由匹配，未知路径继续安全回落到概览页。
- 发布工作流移除 v0.8.3 历史 bootstrap，增加 manifest、文件大小、资源哈希、本地资源与语法校验。
- README 补充 Flaticon 图标来源说明；不改变现有界面、数据语义与交互。

## v0.9.1

- 主题卡说明改为简短中文。
- README 统一为简洁中文说明。
- 本版本仅调整文案，不改界面、图表或数据逻辑。

## v0.9.0

- 实时网速卡恢复 Atlas 原有的单调三次曲线语言，不再使用生硬 polyline。
- 实时网速卡同时绘制上下行平滑曲线，并增加轻量面积渐隐与左右边缘淡化；走势区域保持接近整卡宽度。
- 详情资源历史与三网延迟继续基于真实采样点绘制，并将平滑张力统一到与 Atlas 小图一致的曲线手感。
- 不改变 KPI 字段、COBE 地球、详情 Tooltip / 缩放、流量计算等既有逻辑。

## v0.8.10

- 首页顶部统计按 Monitor 官方默认主题语义调整为 4 张卡：节点、最忙节点、今日流量、实时网速。
- 今日流量同时显示当天上下行与累计总上下行；实时网速上下行共用一张轻量趋势图。
- 移除首页“覆盖地区 / 本周期流量 / 月费折算”统计卡，并删除月费折算聚合逻辑。
- 节点卡继续按 `traffic_mode` 使用 `month_rx / month_tx` 计算本周期已用流量。
- 修正“余量”显示精度：明确使用 `traffic_limit - monthUsage`，大额度场景保留有效小数，避免 1000GB - 少量流量仍显示成 1000GB。
- 预览图由本版本 dist 在本地运行并代理公开 API 后重新截图，确保 README / 后台卡片与当前主题一致。

## v0.8.9

- 修复 v0.8.8 的空白 `preview.png`。
- 预览图改由发布流程直接打开当前 `dash.wxt.me` Atlas 实际页面并截取 1600×900 真实界面，避免二进制转码造成空白图片。
- README 与 Monitor 后台主题卡继续共用根目录同一张 `preview.png`。
- 仅修复预览与版本元数据，不改变主题运行时布局、图表、COBE 地球或 API 行为。

## v0.8.8

- 恢复主题根目录 `preview.png`，供 Monitor 后台主题管理页显示 Atlas 概览缩略图。
- 预览图直接取自当前 Atlas 实际桌面概览界面并裁成 16:9，不使用生成式或虚构 UI。
- README 顶部展示同一张 `preview.png`，主题卡片与仓库说明保持一致。
- 运行时布局、图表交互、COBE 地球与后端 API 均未改变。

## v0.8.7

- 手机详情图默认改为单指按住/左右滑动查看时间游标与 Tooltip，不再把普通触摸直接解释为框选缩放。
- 手机增加显式 `缩放` 模式；开启后拖动才框选时间范围，完成一次缩放后自动回到查看模式，并保留重置缩放。
- PC 保持鼠标悬停查看与拖动框选缩放。
- 手机三网延迟概览改为三等分紧凑卡，三张完整显示；保留线路名、当前延迟、平均值和丢包。
- 延迟图 Y 轴改为基于当前可见线路的自适应区间，不再强制从 0 ms 起；上下各留安全空间并使用整洁刻度。
- 开启削峰或隐藏线路后，Y 轴按处理后的当前可见数据重新计算。
- 延迟图右侧同时显示当前 Y 轴最大值与最小值，避免非零基线造成误判。

## v0.8.6

- 修复 v0.8.5 页脚仍显示 `v0.8.4` 的硬编码版本号。
- PC 与手机详情图的时间轴移出非等比 SVG，改为正常 HTML 文本；解决时间字符被横向压扁的问题，并提升字号与对比度。
- 延迟图右上最大值同样移出 SVG，避免数字被压窄。
- COBE 地区标签加入 12–14px safe inset，靠近边界时自动向内收；密集地区加入纵向错层碰撞处理，重点改善 CN / HK / SG。
- 手机首页 KPI、节点卡与详情资料区轻量压缩，减少无效纵向留白。
- 手机 Global Presence 略微压缩高度，同时保持地球主体尺寸与交互。
- 手机三网延迟摘要改为两列网格，第三项自动整行展示，不再出现右侧卡片被裁切。
- 资源历史分轨进一步收紧高度，保留 Tooltip、拖动缩放与真实时间轴。
- COBE 投影、前后面可见性、节点锚点和后端 API 均未改变。

## v0.8.5

- 详情历史图改为真实时间轴；CPU、RAM、DISK、NETWORK 分轨展示，断线时间不会再被按样本序号压缩。
- 历史图支持悬停数据提示与拖动时间范围缩放，并可一键重置。
- 网络延迟支持点击探针卡隐藏/显示曲线，并加入 Hampel 削峰开关；剩余曲线会自动重算纵轴。
- 桌面端详情 Drawer 标题栏吸顶，滚动历史图时节点名称与关闭按钮保持可见。
- 节点整行支持单击、Tab、Enter/Space 打开详情；保留右侧箭头作为视觉提示。
- 节点列表显示真实连接模式：WebSocket、5 秒轮询或重连状态。
- Geist Sans / Mono / Pixel 全部随主题本地打包，页面不再依赖 jsDelivr 字体 CDN。
- 节点详情补充今日流量、流量重置日与 SWAP 使用信息。
- 静态页面标题恢复为通用 `Monitor`，运行后继续使用站点自定义名称。
- COBE 地球、地区标签、节点布局与后端 API 均保持不变。

## v0.8.4

- 使用本地化 Flaticon Paper Plane（3917567）作为浏览器 favicon 与左上角品牌图标；资源打包在主题内，不依赖图标 CDN。
- 顶栏品牌区只保留纸飞机图标与站点名称，移除 `Atlas / Monitor` 副标题。
- 页面标题与页脚移除 `ATLAS / MONITOR` 品牌文字；保留站点名与版本号。
- 未改动 COBE 地球、节点布局、网络延迟与历史图表逻辑。

## v0.8.3

- Atlas for Monitor moved to its own repository: `wxte/Monitor-Theme-Atlas`.
- Corrected `theme.json.url` so Monitor can resolve future updates from this repository.
- Added automatic GitHub Release publishing from `main` using the version in `theme.json`.
- Release asset name remains exactly `theme.tar.gz` for Monitor's updater.
- No globe, layout, chart, or runtime behavior changes from v0.8.2.

## v0.8.2

- Bound Geist Sans, Geist Mono and Geist Pixel directly with `@font-face`.
- Kept the non-flashing history-chart behavior introduced in v0.8.1.
- Kept COBE globe source and marker-anchor logic unchanged.
